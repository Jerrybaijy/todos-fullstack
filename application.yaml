apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: todos-react-flask-app
  namespace: argocd

spec:
  project: default
  source:
    repoURL: https://github.com/Jerrybaijy/todos-fullstack.git
    targetRevision: HEAD
    path: dev # 指向配置所在的 dev 路径

  destination:
    server: https://kubernetes.default.svc
    namespace: todos-fullstack # 目标命名空间

  syncPolicy:
    syncOptions:
      - CreateNamespace=true 
    automated:
      selfHeal: true
      prune: true